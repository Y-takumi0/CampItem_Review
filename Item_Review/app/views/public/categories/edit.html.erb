<div class="container">
  <div class="row">
    <h4 class="my-5 bg-light">カテゴリー編集</h4>
    <div class="row col-md-12">
      <!-- ジャンル名の更新 -->
      <div class="col-md-6">
      <%= form_with(model: @category, url: category_path(@category), method: :patch, class: 'form-group') do |f| %>
        <%= f.label :name, "カテゴリー", class: 'form-label' %>
      <div class="input-group">
        <%= f.text_field :name, class: 'form-control' %>
        <%= f.submit "更新を保存", class: 'btn btn-success' %>
      </div>
      <% end %>
      </div>
      <!--  -->
    </div>
  </div>
</div>
